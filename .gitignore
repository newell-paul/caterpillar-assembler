# ── Whitelist repo: ignore everything, then re-include what we want ──
*

# Source + build (the .ssd is rebuilt and committed)
!caterpillar.asm
!caterpillar.bas
!caterpillar.asc
!caterpillar.ssd
!caterpillar-original.ssd
!Makefile
!*BOOT

# Browser player (GitHub Pages entry point)
!index.html

# Docs the repo ships
!README.md
!43-years-later.md
!LICENSE

# Keep this whitelist itself
!.gitignore

# Assets: images only (not the 44MB cover PDF)
!assets/
assets/*
!assets/*.png
!assets/*.jpg
!assets/*.jpeg
!assets/*.gif
!assets/README.md
CLAUDE.md
